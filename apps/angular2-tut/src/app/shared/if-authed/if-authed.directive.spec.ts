import { IfAuthedDirective } from './if-authed.directive';

describe('IfAuthedDirective', () => {
  it('should create an instance', () => {
    const directive = new IfAuthedDirective();
    expect(directive).toBeTruthy();
  });
});
